/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // turbopack: {
  //   root: __dirname,
  // },
  reactCompiler: true,
  allowedDevOrigins: ['172.0.0.1'],
};


export default nextConfig;